# Frontend (React + TypeScript)

Minimal starter for React + TypeScript using Vite conventions (files provided, but dependencies are not installed).

To finish setup locally:
1. cd frontend
2. npm install
3. npm run dev

This folder contains a minimal App, entry point, and basic TypeScript config to satisfy TASK-012 scaffold requirements.